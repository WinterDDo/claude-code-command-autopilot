{
  "version": "2026.06",
  "source": "All sequences verified against official docs (plan mode, goal, checkpointing, context-window, sub-agents, routines), 2026-06-11",
  "note": "REFERENCE / inspiration, not triggers. These are powerful command combinations and what each is for. Decide whether one fits the user's current task by reasoning about that task, never by matching a stored condition.",
  "playbooks": [
    {
      "id": "safe-big-change",
      "name_en": "Safe big change",
      "name_zh": "安全大改",
      "steps": ["plan mode (Claude enters it automatically, or Shift+Tab)", "approve the plan", "/goal <success condition>", "/diff to inspect", "if anything breaks: /rewind"],
      "benefit_en": "Big changes get a review gate, a finish line, and an undo button",
      "benefit_zh": "大改动有审查、有标准、有后悔药"
    },
    {
      "id": "long-session-stamina",
      "name_en": "Long session stamina",
      "name_zh": "长会话续航",
      "steps": ["/context to see usage", "/compact <what to keep> at a phase boundary", "continue the same task with room to breathe"],
      "benefit_en": "Outlive the context window without losing the decisions that matter",
      "benefit_zh": "活过上下文上限，关键决定一个不丢"
    },
    {
      "id": "parallel-explore",
      "name_en": "Parallel exploration",
      "name_zh": "并行探索",
      "steps": ["/branch to try option A yourself", "/fork <directive> to send a context-aware agent after option B", "main thread keeps moving"],
      "benefit_en": "Compare two approaches without betting the session on either",
      "benefit_zh": "两个方案同时试，主线不押注"
    },
    {
      "id": "pre-heavy-tuneup",
      "name_en": "Pre-heavy-task tuneup",
      "name_zh": "重活前调校",
      "steps": ["/usage to check headroom", "/model or /effort to match the task", "/advisor on for a second opinion", "then start"],
      "benefit_en": "Expensive work starts with the right brain and a safety reviewer",
      "benefit_zh": "贵的活配对的脑子，还有军师盯着"
    },
    {
      "id": "clean-finish",
      "name_en": "Clean finish",
      "name_zh": "收尾三连",
      "steps": ["/export to keep the record", "/recap or /insights for the summary", "/clear for the next task (old session stays reachable via /resume)"],
      "benefit_en": "Close a task with a record, a summary, and a clean desk",
      "benefit_zh": "留档、总结、清桌，一气呵成"
    },
    {
      "id": "aside-isolation",
      "name_en": "Aside isolation",
      "name_zh": "题外话隔离",
      "steps": ["/btw <question> any time mid-task"],
      "benefit_en": "Curiosity answered, zero pollution of the main thread",
      "benefit_zh": "好奇心满足了，主线一点不脏"
    },
    {
      "id": "unattended",
      "name_en": "Unattended work",
      "name_zh": "无人值守",
      "steps": ["/schedule for recurring jobs (Claude can set this up for you)", "/background to detach a long task", "/tasks to check progress later"],
      "benefit_en": "Work continues while you sleep; you just read the results",
      "benefit_zh": "你睡觉它干活，醒来看结果"
    },
    {
      "id": "mass-change",
      "name_en": "Mass change",
      "name_zh": "大规模并改",
      "steps": ["describe the repetitive change across many files", "Claude orchestrates parallel agents (or /batch)", "/workflows to watch progress"],
      "benefit_en": "A hundred-file chore becomes one instruction plus a progress bar",
      "benefit_zh": "百个文件的体力活，一句话加一个进度条"
    }
  ]
}
